import './assets/main.css' // importei o css
import { createApp } from 'vue' // importei a funcao createApp do pacote vue
import App from './App.vue' //importei o componente
createApp(App).mount('#app') // exibi o meu componente (RAIZ) a partir da DIV com id app no index
